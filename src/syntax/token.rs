#[derive(Copy, Clone, Eq, PartialEq, Debug)]
pub enum Token<'src> {
    Pi,
    Lambda,
    UnitType,
    UnitVal,
    ThinArrow,
    ThickArrow,
    OpenParen,
    CloseParen,
    OpenBracket,
    CloseBracket,
    Equals,
    Comma,
    Colon,
    Zero,
    One,
    Meet,
    Join,
    Inv,
    Path,
    PathBind,
    VariableName(&'src str),
}
